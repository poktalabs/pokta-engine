{
  "name": "@pokta-engine/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p pricing/tsconfig.json --noEmit",
    "build": "tsc --noEmit && tsc -p pricing/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pokta-engine/contract": "workspace:*",
    "@pokta-engine/db": "workspace:*",
    "@pokta-engine/integrations": "workspace:*",
    "@pokta-engine/llm": "workspace:*",
    "drizzle-orm": "^0.38.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
