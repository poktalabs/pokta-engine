{
  "name": "@godin-engine/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p pricing/tsconfig.json --noEmit",
    "build": "tsc --noEmit && tsc -p pricing/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@godin-engine/contract": "workspace:*",
    "@godin-engine/db": "workspace:*",
    "@godin-engine/llm": "workspace:*",
    "@godin-engine/mercadolibre": "workspace:*",
    "@godin-engine/notion": "workspace:*",
    "@godin-engine/resend": "workspace:*",
    "@godin-engine/shopify": "workspace:*",
    "drizzle-orm": "^0.38.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
